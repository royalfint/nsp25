<% include ../dheader %>

<form class="prod" action="/dashboard/complexes/<%= post.id %>?_method=PUT" method="POST">
    <h1>Редактирование Программы</h1>
    <input type="text" placeholder="Имя новой программы" name="name" value="<%= post.name %>">
    <input type="text" placeholder="Ссылка на картинку программы" name="img" value="<%= post.image %>">
    <input type="text" placeholder="Краткое описание" name="short" value="<%= post.short %>">
    <textarea placeholder="Содержание программы" name="desc"><%- post.desc %></textarea>
    <div class="flash" style="margin: 1.5rem 0 0 0; ">
        <% if(error && error.length > 0) { %>
                <div class="alert alert-danger" role="alert">
                    <%= error %>
                </div>
        <% } if(success && success.length > 0) { %>
            <div class="alert alert-success" role="alert">
                <%= success %>
            </div>
        <% } %>
    </div>
    <button class="smooth">Обновить</button>
</form>

<% include ../dfooter %>